[project]
name = "answersnap-backend"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic",
    "opencv-python-headless",
    "numpy",
]
